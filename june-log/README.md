# june-log

Snapshot of the previous version of the investor deck (`deck.html`) as it stood in
June 2026, preserved before the redesign to the new desktop/mobile flow.

`june-log/deck.html` is a frozen copy — kept for reference only. The live deck is the
`deck.html` at the repository root.
